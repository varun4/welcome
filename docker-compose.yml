services:
  caddy:
    image: caddy:alpine
    ports:
      - "80:80"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ./games:/srv:ro
    environment:
      - ACTIVE_GAME=porco
    restart: unless-stopped
